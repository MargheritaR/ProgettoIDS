package it.unicam.IDS.progetto.Operazioni;

import it.unicam.IDS.progetto.Entita.Utente;

import java.util.ArrayList;

public class Autenticazione {
/*
    public void Login(ArrayList<Utente> listaUtente, Utente utente) {
        if (!listaUtente.contains(utente))
            System.out.println("Utente non trovato");
        if (FindUser(listaUtente, utente))
            System.out.println("Credeziali corrette");
        else
            System.out.println("Credenziali non corrette");
    }

    private boolean FindUser(ArrayList<Utente> listaUtente, Utente utente) {
        for (Utente u : listaUtente) {
            if (u.getEmail() == utente.getEmail() && u.getPassword().equals(utente.getPassword()))
                return true;
        }
        return false;
    }

 */
}
